# Binance Account Management - Feature Flow Diagram

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        USER INTERACTION FLOW                            │
└─────────────────────────────────────────────────────────────────────────┘

┌──────────────┐
│   Browser    │
│ bot.winu.app │
└──────┬───────┘
       │
       │ 1. Login (admin/admin123)
       ▼
┌────────────────────┐
│   Dashboard Page   │
│  ┌──────────────┐  │
│  │  Header      │  │
│  │  [API Keys]  │◄─── Click this button!
│  └──────────────┘  │
└────────┬───────────┘
         │
         │ 2. Modal Opens
         ▼
┌────────────────────────────────────────────────────────┐
│              API Keys Management Modal                 │
│                                                        │
│  ┌──────────────┬──────────────────┐                  │
│  │ My Accounts  │ Add New Account  │                  │
│  └──────────────┴──────────────────┘                  │
│                                                        │
│  ┌────────────────────────────────────────┐           │
│  │ 🔑 Test Trading Account                │           │
│  │ ✓ Verified  | 🟢 Active | 🟡 Testnet  │           │
│  │                                        │           │
│  │ API Key: ABC...XYZ                     │           │
│  │ Type: FUTURES                          │           │
│  │ Max Position: $1000 | Leverage: 10x    │           │
│  │ Balance: $5,234.56 | PnL: +$124.32     │           │
│  │                                        │           │
│  │ [Verify] [Refresh] [Enable] [Delete]  │           │
│  └────────────────────────────────────────┘           │
└────────────────────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────────────┐
│                     BACKEND PROCESSING FLOW                             │
└─────────────────────────────────────────────────────────────────────────┘

User Action ──┐
              │
              ▼
     ┌─────────────────┐
     │  Dashboard JS   │
     │  (Alpine.js)    │
     └────────┬────────┘
              │ API Call
              │ fetch('/api/bot/multi-account/...')
              ▼
     ┌─────────────────┐
     │   FastAPI       │
     │   (port 8001)   │
     └────────┬────────┘
              │
              │ Authenticate Session
              ▼
     ┌─────────────────┐
     │  Multi-Account  │
     │   Router        │
     │  (routers/      │
     │   multi_account_│
     │   trading.py)   │
     └────────┬────────┘
              │
              ▼
    ┌──────────────────┐
    │ Encryption       │
    │ Service          │
    │ (Fernet AES-128) │
    └────────┬─────────┘
             │
             │ Encrypt/Decrypt
             ▼
    ┌──────────────────┐
    │  PostgreSQL DB   │
    │  user_api_keys   │
    │  table           │
    └────────┬─────────┘
             │
             │ Store/Retrieve
             ▼
    ┌──────────────────┐
    │  Binance API     │
    │  (Verify/Balance)│
    └──────────────────┘


┌─────────────────────────────────────────────────────────────────────────┐
│                       DATA FLOW EXAMPLES                                │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│  1. ADD API KEY                                                         │
└─────────────────────────────────────────────────────────────────────────┘

User Input:
  ├─ API Key: "SaF4OnvK1dhNMmeD..."
  ├─ API Secret: "ujc2eBsOcy8OZy..."
  ├─ Account Name: "Test Account"
  ├─ Type: Futures
  └─ Testnet: Yes

    ↓

Frontend (addApiKey()):
  POST /api/bot/multi-account/api-keys
  {
    "api_key": "SaF4OnvK1dhNMmeD...",
    "api_secret": "ujc2eBsOcy8OZy...",
    "api_name": "Test Account",
    "account_type": "futures",
    "test_mode": true,
    ...
  }

    ↓

Backend (create_api_key()):
  1. Encrypt API key & secret
  2. Store in database:
     api_key_encrypted: "gAAAAABh..."
     api_secret_encrypted: "gAAAAABh..."
  3. Return success + masked key

    ↓

Response to Frontend:
  {
    "success": true,
    "api_key_id": 1,
    "api_key_masked": "SaF...7FZ"
  }

    ↓

UI Updates:
  - Switch to "My Accounts" tab
  - Show new account card
  - Display success message in activity feed


┌─────────────────────────────────────────────────────────────────────────┐
│  2. VERIFY API KEY                                                      │
└─────────────────────────────────────────────────────────────────────────┘

User Clicks: [Verify] button

    ↓

Frontend (verifyApiKey(id)):
  POST /api/bot/multi-account/api-keys/{id}/verify

    ↓

Backend (verify_api_key()):
  1. Get encrypted credentials from DB
  2. Decrypt API key & secret
  3. Create Binance exchange client
  4. Call: exchange.fetch_balance()
  5. Update DB:
     - is_verified = TRUE
     - last_verified_at = NOW()
  6. Return balance

    ↓

Response:
  {
    "success": true,
    "balance": 5234.56
  }

    ↓

UI Updates:
  - Show "✓ Verified" badge
  - Display balance: $5,234.56
  - Activity feed: "✅ API key verified successfully. Balance: $5234.56"


┌─────────────────────────────────────────────────────────────────────────┐
│  3. ENABLE AUTO-TRADING                                                 │
└─────────────────────────────────────────────────────────────────────────┘

User Clicks: [Enable Trading] button

    ↓

Frontend (toggleAutoTrade(id, true)):
  PATCH /api/bot/multi-account/api-keys/{id}
  {
    "auto_trade_enabled": true
  }

    ↓

Backend (update_api_key()):
  UPDATE user_api_keys 
  SET auto_trade_enabled = TRUE,
      updated_at = NOW()
  WHERE id = {id} AND user_id = {user_id}

    ↓

Response:
  {
    "success": true
  }

    ↓

UI Updates:
  - Button changes to [Disable Trading]
  - Button color changes (yellow)
  - Activity feed: "✅ Enabled auto-trading"

    ↓

Bot Behavior:
  - When signal generated, bot checks:
    SELECT * FROM user_api_keys 
    WHERE auto_trade_enabled = TRUE
  - Executes trade on all enabled accounts
  - Uses account-specific settings (leverage, position size, etc.)


┌─────────────────────────────────────────────────────────────────────────┐
│                        SECURITY FLOW                                    │
└─────────────────────────────────────────────────────────────────────────┘

Plain API Key               Encrypted Storage           Display
─────────────               ─────────────────           ───────

"SaF4OnvK1dhNMmeD...XYZ"   
       │                                                     
       │ Fernet Encrypt                                     
       │ (AES-128)                                          
       ▼                                                     
"gAAAAABhXYZ...encrypted"  ──┬──> Stored in DB
                             │    (api_key_encrypted)
                             │
                             │ Decrypt only when needed:
                             │ - Verification
                             │ - Balance check
                             │ - Trading
                             │
                             └──> Mask for display
                                  "SaF...XYZ"


┌─────────────────────────────────────────────────────────────────────────┐
│                    MULTI-ACCOUNT TRADING FLOW                           │
└─────────────────────────────────────────────────────────────────────────┘

Trading Signal Generated (e.g., BTC/USDT LONG)
       │
       ▼
┌─────────────────┐
│  Bot Executor   │
└────────┬────────┘
         │
         │ Query: Get all accounts with auto_trade_enabled = TRUE
         ▼
┌────────────────────────────────────────────────┐
│  User has 3 accounts:                          │
│  1. Main Account   (auto_trade: ON)  ✓         │
│  2. Test Account   (auto_trade: OFF) ✗         │
│  3. Backup Account (auto_trade: ON)  ✓         │
└────────┬───────────────────────────────────────┘
         │
         │ Execute on accounts 1 & 3 only
         ▼
┌────────────────────────────────────────┐
│ Account 1: Main Account                │
│ - Decrypt API keys                     │
│ - Position size: $1000 (from settings) │
│ - Leverage: 20x (from settings)        │
│ - Execute: BUY BTC/USDT                │
│ - Store order in multi_account_orders  │
└────────────────────────────────────────┘
         │
         ▼
┌────────────────────────────────────────┐
│ Account 3: Backup Account              │
│ - Decrypt API keys                     │
│ - Position size: $500 (from settings)  │
│ - Leverage: 10x (from settings)        │
│ - Execute: BUY BTC/USDT                │
│ - Store order in multi_account_orders  │
└────────────────────────────────────────┘
         │
         ▼
┌────────────────────────────────────────┐
│  Track Results:                        │
│  - Account 1: +$45.23 PnL              │
│  - Account 3: +$22.11 PnL              │
│  - Update total_pnl in user_api_keys   │
└────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────────────┐
│                    DATABASE RELATIONSHIPS                               │
└─────────────────────────────────────────────────────────────────────────┘

┌──────────────┐
│    users     │
│              │
│  id  (PK)    │───┐
│  username    │   │
│  email       │   │
└──────────────┘   │
                   │ Foreign Key
                   ▼
           ┌───────────────────┐
           │  user_api_keys    │
           │                   │
           │  id  (PK)         │
           │  user_id  (FK)────┘
           │  api_key_encrypted│
           │  api_secret_enc   │
           │  api_name         │
           │  account_type     │
           │  test_mode        │
           │  is_active        │
           │  is_verified      │
           │  auto_trade_en    │
           │  max_position_$   │
           │  leverage         │
           │  current_balance  │
           │  total_pnl        │
           └─────────┬─────────┘
                     │ Foreign Key
                     ▼
           ┌───────────────────┐
           │ multi_account_    │
           │    orders         │
           │                   │
           │  id  (PK)         │
           │  api_key_id  (FK)─┘
           │  symbol           │
           │  side             │
           │  quantity         │
           │  price            │
           │  status           │
           │  pnl              │
           └───────────────────┘


┌─────────────────────────────────────────────────────────────────────────┐
│                      FILES & DIRECTORIES                                │
└─────────────────────────────────────────────────────────────────────────┘

/home/ubuntu/winubotsignal/
│
├── bot/dashboard/
│   ├── app.py                              ← UI & JavaScript (MODIFIED)
│   ├── BINANCE_ACCOUNTS_IMPLEMENTATION.md  ← Implementation docs (NEW)
│   └── TEST_BINANCE_ACCOUNTS.md            ← Testing guide (NEW)
│
├── apps/api/
│   └── routers/
│       └── multi_account_trading.py        ← API endpoints (EXISTS)
│
├── migrations/
│   └── create_multi_account_trading.sql    ← Database schema (EXISTS)
│
├── docker-compose.yml                      ← Updated with encryption key
├── production.env                          ← Contains API_KEY_ENCRYPTION_KEY
├── test_binance_accounts.sh               ← Test script (NEW)
└── BINANCE_ACCOUNTS_COMPLETE.md           ← This summary (NEW)


┌─────────────────────────────────────────────────────────────────────────┐
│                      QUICK START GUIDE                                  │
└─────────────────────────────────────────────────────────────────────────┘

  Step 1: Visit Dashboard
  ┌────────────────────────────────────┐
  │  https://bot.winu.app              │
  │  Username: admin                   │
  │  Password: admin123                │
  └────────────────────────────────────┘
           │
           ▼
  Step 2: Click "API Keys" Button
  ┌────────────────────────────────────┐
  │  In header, find purple button     │
  │  with key icon labeled "API Keys"  │
  └────────────────────────────────────┘
           │
           ▼
  Step 3: Add Testnet API Key
  ┌────────────────────────────────────┐
  │  Get keys from:                    │
  │  https://testnet.binance.vision/   │
  │                                    │
  │  Fill form and click               │
  │  "Add API Key"                     │
  └────────────────────────────────────┘
           │
           ▼
  Step 4: Verify & Test
  ┌────────────────────────────────────┐
  │  Click "Verify" button             │
  │  Click "Refresh Balance"           │
  │  Click "Enable Trading"            │
  └────────────────────────────────────┘
           │
           ▼
         🎉 Done!


┌─────────────────────────────────────────────────────────────────────────┐
│                    IMPLEMENTATION STATUS: ✅ COMPLETE                   │
└─────────────────────────────────────────────────────────────────────────┘

All features are implemented and ready to use!
Start testing with Binance testnet keys today.

For detailed documentation, see:
- BINANCE_ACCOUNTS_COMPLETE.md
- bot/dashboard/BINANCE_ACCOUNTS_IMPLEMENTATION.md
- bot/dashboard/TEST_BINANCE_ACCOUNTS.md
```



